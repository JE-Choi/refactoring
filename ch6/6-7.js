// a라는 변수는 가독성이 없다. (a → area)
let area = width * height; // 너비와 높이라고 자주 말하니까, 순서변경

// 축약어 지양
const companyName = '애플';

// 유추해야 하는 이름 지양 → 구체적으로 작성
let movieTitle = '제목없음';
let result = `<h1>${movieTitle}</h1>`;
